---
name: commit
description: Stage, commit with descriptive message, and push changes
---

1. Run `git status` to see changes
2. Run appropriate tests/linting for modified files
3. Stage changes with `git add`
4. Create commit with descriptive message summarizing the changes
5. Push to remote with `git push`
6. Verify with `git status`
